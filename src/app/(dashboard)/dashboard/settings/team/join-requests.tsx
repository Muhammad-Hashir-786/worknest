"use client";
import { useActionState } from "react";
import { reviewJoinRequestAction, type JoinRequestActionState } from "~/actions/join-request";
import type { JoinRequestSummary } from "~/services/join-request";
const initial: JoinRequestActionState = {};
export default function JoinRequests({ requests }: { requests: JoinRequestSummary[] }) { const [state, action, pending] = useActionState(reviewJoinRequestAction, initial); return <div className="space-y-3">{requests.length === 0 ? <p className="rounded-xl bg-neutral-50 p-4 text-sm text-neutral-500">No pending join requests.</p> : requests.map((request) => <div key={request.id} className="rounded-xl border border-neutral-100 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-semibold text-neutral-900">{request.userName}</p><p className="text-xs text-neutral-500">{request.userEmail} · requested {new Date(request.createdAt).toLocaleDateString()}</p>{request.message && <p className="mt-2 text-sm text-neutral-600">“{request.message}”</p>}</div><div className="flex gap-2"><form action={action}><input type="hidden" name="requestId" value={request.id} /><input type="hidden" name="decision" value="approved" /><button disabled={pending} className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-bold text-white disabled:opacity-50">Approve as member</button></form><form action={action}><input type="hidden" name="requestId" value={request.id} /><input type="hidden" name="decision" value="rejected" /><button disabled={pending} className="rounded-lg border border-neutral-200 px-3 py-2 text-xs font-bold text-neutral-600 disabled:opacity-50">Decline</button></form></div></div></div>)}{state.error && <p className="text-sm font-semibold text-red-700">{state.error}</p>}{state.success && <p className="text-sm font-semibold text-emerald-700">{state.success}</p>}</div>; }
