"use client";
import { useActionState } from "react";
import { submitJoinRequest, type JoinRequestActionState } from "~/actions/join-request";
const initial: JoinRequestActionState = {};
export default function JoinRequestForm() { const [state, action, pending] = useActionState(submitJoinRequest, initial); return <div className="mt-6 rounded-2xl border border-red-100 bg-red-50/50 p-4"><p className="text-sm font-bold text-neutral-900">Request access by organization handle</p><p className="mt-1 text-xs leading-5 text-neutral-500">Search with a unique handle such as <span className="font-semibold">@silverloft</span> or use the organization name. You become a member only after approval.</p><form action={action} className="mt-4 space-y-2"><input name="organizationName" required placeholder="@organization-handle" className="w-full rounded-xl border border-neutral-200 bg-white px-3 py-2.5 text-sm" /><textarea name="message" maxLength={500} placeholder="Optional: tell them who you are or which team you work with" className="min-h-20 w-full rounded-xl border border-neutral-200 bg-white px-3 py-2.5 text-sm" /><button disabled={pending} className="rounded-xl bg-[#d92d27] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">{pending ? "Sending request…" : "Send join request"}</button></form>{state.error && <p className="mt-3 text-sm font-semibold text-red-700">{state.error}</p>}{state.success && <p className="mt-3 rounded-xl bg-emerald-50 p-3 text-sm font-semibold text-emerald-700">{state.success}</p>}</div>; }
