export default function StatCard({ label, value, accent = false }: { label: string; value: number; accent?: boolean }) { return <div className={`worknest-panel rounded-2xl border p-5 ${accent ? "border-[#d92d27] bg-[#d92d27] text-white" : "border-neutral-200 bg-white"}`}><p className={`text-sm ${accent ? "text-red-100" : "text-neutral-500"}`}>{label}</p><p className="mt-3 text-3xl font-bold tracking-tight">{value}</p></div>; }
