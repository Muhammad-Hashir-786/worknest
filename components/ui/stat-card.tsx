export default function StatCard({ label, value, accent = false }: { label: string; value: number; accent?: boolean }) { return <div className={`worknest-panel group relative overflow-hidden rounded-2xl border p-5 transition duration-200 hover:-translate-y-0.5 hover:shadow-lg ${accent ? "border-[#d92d27] bg-[#d92d27] text-white" : "border-neutral-200 bg-white"}`}><span className={`absolute -right-6 -top-8 h-24 w-24 rounded-full blur-2xl ${accent ? "bg-white/15" : "bg-red-100/60"}`} /><p className={`relative text-xs font-bold uppercase tracking-[0.14em] ${accent ? "text-red-100" : "text-neutral-400"}`}>{label}</p><p className="relative mt-3 text-3xl font-bold tracking-tight">{value}</p><span className={`relative mt-3 block h-1 w-8 rounded-full ${accent ? "bg-white/50" : "bg-[#d92d27]"}`} /></div>; }
